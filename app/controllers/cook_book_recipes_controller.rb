class CookBookRecipesController < ApplicationController
    def destroy
         #@user = current_user
         #cook_book = CookBook.find(params[:id])
           #cook_book = @user.cook_books.select do |this_book|  #can't grab the recipe id here
            #            this_book.recipes.include?(rc)
            #           end
          # byebug            
        #cook_book.delete(rc)
        #redirect_to cook_book_path(cook_book)         
    end
end